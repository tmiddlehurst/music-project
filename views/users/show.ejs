<h1><%= user.username %></h1>

<img src="<%=user.image%>">

<div class = "container"></div>
	<% for(var i = 0; i < user.bands.length; i++ ) { %>
		<% console.log(user.bands.lenth); %>
		<div class = "col-sm-6">		
			<div class = "card">

				<div class = "card-block">

					<h3 class = "card-title"><a href="/<%= user.bands[i].id %>"><%= user.bands[i].name %></a></h3>

				</div>	
			</div>
		</div>
	<% } %>
</div>